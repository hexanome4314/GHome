Flag de compilation :
CPPFLAGS= `xml2-config --cflags`
LDFLAGS = `xml2-config --libs`

Makefile perso utilisé :
CC      = gcc
CFLAGS  = -g -Wall
CPPFLAGS= `xml2-config --cflags`
LDFLAGS = `xml2-config --libs`
SRC     = $(wildcard *.c)
EXE     = $(SRC:.c=.o)

all:    $(EXE)

clean:
        rm *.o

%.o: %.c
        $(CC) $(CFLAGS) $< -o $@ $(CPPFLAGS) $(LDFLAGS)

